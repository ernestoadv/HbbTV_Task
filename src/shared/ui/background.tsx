import styled from "styled-components";

const Background = styled.div`
  background: linear-gradient(to bottom, #0E1521, #000000);
  position: fixed;
  width: 100vw;
  height: 100vh;
`;

export default Background;
