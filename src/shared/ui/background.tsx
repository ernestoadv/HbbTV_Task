import styled from "styled-components";

const Background = styled.div`
  background: linear-gradient(to bottom, #0E1521, rgba(0, 0, 0, 1));
  position: fixed;
  width: 100vw;
  height: 100vh;
`;

export default Background;
